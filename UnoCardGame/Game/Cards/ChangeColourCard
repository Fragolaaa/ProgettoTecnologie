package Game.Cards;

public class ChangeColorCard extends Card{
    int color = -1; //colore selezionato

    @Override
    public void accept(CardVisitor visitor) {
        visitor.visit(this);
    }

}
